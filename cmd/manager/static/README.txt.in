============================================
  ddns-manager Windows 节点安装包
  Version: v__VERSION__
  Lanxun CO.,Ltd.
============================================

【功能介绍】
本安装包用于在 Windows 系统上安装 ddns-manager 节点客户端。
安装后将自动注册为 Windows 服务，每 5 分钟向管理端上报心跳和
DNS 更新状态，实现 DDNS 集中管理。

【文件说明】
  install.bat            — 安装启动器（右键→管理员运行）
  ddns-installer.exe     — Go 安装向导（交互式）
  node-agent.exe         — 节点守护进程

【安装步骤】
  1. 将 zip 内所有文件解压到同一目录
  2. 右键 install.bat → 以管理员身份运行
  3. 按向导提示输入：
     a. 管理端地址（如 https://your-server.com:30443）
     b. 安装目录（默认 C:\ddns-manager）
     c. 节点名称（如 win-pc，不可与其他节点重名）
  4. 向导自动完成：注册→安装服务→启动
  5. 登录管理端 WebUI → 节点页面 → 找到此节点 → 点击「分配」→ 配置 DNS

【注意事项】
  · 如已安装 ddns-go，向导会提示冲突并要求清除（两者不能同时运行）
  · 安装目录会自动创建，旧版会自动清理（配置保留）
  · 安装完成后的节点只是在线，需在管理端配置 DNS 才会开始 DDNS

【卸载】
  以管理员身份运行：
    C:\ddns-manager\ddns-installer.exe -uninstall
